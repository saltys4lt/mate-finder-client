import axios, { CancelToken } from 'axios';
import ClientUser from '../../types/ClientUser';
const baseUrl = import.meta.env.VITE_BASE_URL;

export const fetchUpdatedTeam = async (cancelToken: CancelToken) => {};
